{
  "name": "d9n.coding",
  "version": "0.1.0",
  "description": "npm managed scripts for fetching components and building the site",
  "main": "index.js",
  "scripts": {
    "firstrun": "bower install && npm run updatedeps && npm install",
    "updatedeps": "ruby scripts/copy_bower_dependencies.rb",
    "serve": "node_modules/watchy/bin/watchy -w _config.yml -- jekyll serve",
    "tidy": "find _site -name '*.html' -type f -exec tidy --drop-empty-elements no -m -utf8 -i '{}' \\;",
    "build": "jekyll build",
    "build-release": "export JEKYLL_ENV=production && npm run build && npm run tidy && cp _ignore/SITE_README.md _site/README.md"
  },
  "author": "David Herman",
  "license": "MIT",
  "dependencies": {
    "watchy": "^0.6.5"
  }
}
